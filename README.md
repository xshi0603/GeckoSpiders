# GeckoSpiders
<h2>interface Deque<E>  </h2>-- to be implemented in class QQ
  Methods
    addFirst(E x);
    addLast(E x);
    removeFirst();
    removeLast();
    peekFirst();
    peekLast();
    
    
class QQ implements Deque<E>
  Methods
    addFirst(E x)
      
